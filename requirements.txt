# E1 safe stack for Pythia/GPT-NeoX checkpoint analysis.
# We pin transformers below recent FP8 integration changes that can import
# torch.float8_e8m0fnu on environments whose PyTorch does not provide it.
torch>=2.1,<2.8
transformers==4.40.2
accelerate==0.30.1
safetensors>=0.4.3
huggingface_hub>=0.23,<1.0
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
pyyaml>=6.0
tqdm>=4.66

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "critical-periods"
version = "0.1.0"
description = "Experiments for developmental phases and critical periods in LLM pretraining"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
